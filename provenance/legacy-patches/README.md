# Legacy patch archive

This directory preserves the original Ascend PR #272 commit as an unmodified
`git format-patch` export. Author metadata, message, trailers, and exact diff are
retained.

The implementation derives from MIT-licensed `scale-snu/layered-prefill` code;
copyright and license notices must remain attached to derived code.
